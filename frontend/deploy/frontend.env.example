# Production: скопируйте на сервер как:
#   /srv/casino/shared/frontend/env/frontend.env
# и подставьте свои значения (chmod 600).
#
# NEXT_PUBLIC_* уже зашиваются при сборке в GitHub Actions из secrets.
# Здесь задаётся то, что читает systemd / process при старте Node (standalone).

NODE_ENV=production
PORT=3000
